# trace-redactor

Redact secrets/PII from agent traces before sharing.

## Install

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -e .
```

## Usage

```bash
trace-redactor --task "demo"
```
